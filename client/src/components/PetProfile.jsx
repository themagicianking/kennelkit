import { Card } from "@material-tailwind/react";

export default function PetProfile({
  petname,
  species,
  breed,
  sex,
  altered,
  birthday,
  weight,
  physicaldesc,
}) {
  return <Card></Card>;
}
